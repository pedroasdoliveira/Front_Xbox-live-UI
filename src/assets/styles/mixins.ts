import { css } from 'styled-components'

const title = () => css`
    font-family: 'Times New Roman', Times, serif;
    font-size: '1.6rem';
    line-height: '140%';
`

export const mixins = {
    title,
}